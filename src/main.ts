import { registerRootComponent } from "expo";
import App from "./App";

/**
 * Sets the initial React component to render natively in the app's
 * root React Native view on Android, iOS, tvOS and the web.
 */
registerRootComponent(App);
